machine:
  environment:
    LANG: en_US.UTF-8
dependencies:
  pre:
    - sudo gem install cocoapods --version 0.35.0
    - pod setup
    - pod install
test:
 override:
    - ./scripts/cli/bin/cli build --cli

